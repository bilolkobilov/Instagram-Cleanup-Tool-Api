# Add any Flask extensions here (e.g., SQLAlchemy, Marshmallow, etc.)

def register_extensions(app):
    """
    Register Flask extensions
    """
    # Example:
    # db.init_app(app)
    # migrate.init_app(app, db)
    pass